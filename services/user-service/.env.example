# User Service — owns the User DB (users, tenants, plans).
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/travelmanager_user
NITRO_PORT=3001
# Local dev without the gateway: inject a fake identity.
DEV_USER_UID=
DEV_USER_EMAIL=dev@example.com
DEV_USER_NAME=Dev User
