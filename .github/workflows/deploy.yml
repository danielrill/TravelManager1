name: build-and-deploy

on:
  push:
    branches: [main]
    paths:
      - 'services/**'
      - 'packages/**'
      - 'app/**'
      - 'k8s/**'
      - 'tests/**'
      - 'Dockerfile'
      - 'Dockerfile.service'
      - 'vitest.config.js'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

env:
  REGION: europe-west1
  CLUSTER: travelmanager-gke

permissions:
  contents: read
  id-token: write   # Workload Identity Federation

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - run: npm install
      - name: Unit tests
        run: npm run test
      - name: Build all services (compile check)
        run: |
          for s in api-gateway user-service trip-service destination-service \
                   social-service travel-info-service notification-service; do
            npm run build --workspace services/$s
          done

  images:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - api-gateway
          - user-service
          - trip-service
          - destination-service
          - social-service
          - travel-info-service
          - notification-service
    steps:
      - uses: actions/checkout@v4
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.DEPLOY_SA }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${REGION}-docker.pkg.dev --quiet
      - name: Build & push ${{ matrix.service }}
        run: |
          IMG="${REGION}-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/travelmanager/${{ matrix.service }}"
          docker build -f Dockerfile.service --build-arg SERVICE=${{ matrix.service }} \
            -t "$IMG:${GITHUB_SHA}" -t "$IMG:latest" .
          docker push "$IMG:${GITHUB_SHA}"
          docker push "$IMG:latest"

  frontend:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.DEPLOY_SA }}
      - uses: google-github-actions/setup-gcloud@v2
      - run: gcloud auth configure-docker ${REGION}-docker.pkg.dev --quiet
      - name: Build & push frontend
        run: |
          IMG="${REGION}-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/travelmanager/frontend"
          docker build -t "$IMG:${GITHUB_SHA}" -t "$IMG:latest" .
          docker push "$IMG:${GITHUB_SHA}"
          docker push "$IMG:latest"

  deploy:
    needs: [images, frontend]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.DEPLOY_SA }}
      - uses: google-github-actions/setup-gcloud@v2
        with:
          install_components: gke-gcloud-auth-plugin
      - uses: azure/setup-helm@v4
      - name: Get cluster credentials
        run: gcloud container clusters get-credentials "$CLUSTER" --region "$REGION" --project "${{ secrets.GCP_PROJECT }}"
      - name: Helm upgrade
        run: |
          # Public NUXT_* config (Firebase web config + Google Maps browser key)
          # reaches every pod as runtime env via global.extraEnv. These are
          # client-side public values (they ship in the SPA bundle); the Maps
          # browser key is protected by HTTP-referrer restriction, not secrecy.
          # Sourced from GitHub Actions secrets so they stay out of git.
          helm upgrade --install travelmanager k8s/travelmanager \
            --set global.imageRegistry=${REGION}-docker.pkg.dev/${{ secrets.GCP_PROJECT }}/travelmanager \
            --set global.imageTag=${GITHUB_SHA} \
            --set global.gcpProject=${{ secrets.GCP_PROJECT }} \
            --set global.cloudSqlInstance=${{ secrets.CLOUD_SQL_INSTANCE }} \
            --set serviceAccount.gcpServiceAccount=travelmanager-gke@${{ secrets.GCP_PROJECT }}.iam.gserviceaccount.com \
            --set-string "global.extraEnv[0].name=NUXT_PUBLIC_GOOGLE_MAPS_KEY" \
            --set-string "global.extraEnv[0].value=${{ secrets.NUXT_PUBLIC_GOOGLE_MAPS_KEY }}" \
            --set-string "global.extraEnv[1].name=NUXT_PUBLIC_FIREBASE_API_KEY" \
            --set-string "global.extraEnv[1].value=${{ secrets.NUXT_PUBLIC_FIREBASE_API_KEY }}" \
            --set-string "global.extraEnv[2].name=NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN" \
            --set-string "global.extraEnv[2].value=${{ secrets.NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}" \
            --set-string "global.extraEnv[3].name=NUXT_PUBLIC_FIREBASE_PROJECT_ID" \
            --set-string "global.extraEnv[3].value=${{ secrets.NUXT_PUBLIC_FIREBASE_PROJECT_ID }}" \
            --set-string "global.extraEnv[4].name=NUXT_PUBLIC_FIREBASE_APP_ID" \
            --set-string "global.extraEnv[4].value=${{ secrets.NUXT_PUBLIC_FIREBASE_APP_ID }}" \
            --set-string "global.extraEnv[5].name=NUXT_PUBLIC_FIREBASE_STORAGE_BUCKET" \
            --set-string "global.extraEnv[5].value=${{ secrets.NUXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}" \
            --wait --timeout 10m
